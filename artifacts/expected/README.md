# Expected Outputs

These JSON snapshots are the canonical CPU outputs for the practitioner demos in
`examples/`, captured with `seed=0`.

They serve two purposes:

- give readers a known-good reference for what the demos should produce
- let the test suite catch accidental behavior drift in the book companion

The values are intentionally small and synthetic. They are not benchmark claims
or production metrics.
